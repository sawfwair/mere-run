# Flux2Klein Transformer

Native FLUX.2 transformer implementation.

- `Flux2Transformer.swift`: transformer entry point.
- `Flux2EmbedND.swift`: positional embedding support.

This directory is model math. Avoid adding filesystem, CLI, or model-store
concerns here.
