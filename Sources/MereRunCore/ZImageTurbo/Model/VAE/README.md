# ZImageTurbo VAE

VAE implementation for ZImage Turbo.

- `AutoencoderKL.swift`: autoencoder entry point and encode/decode behavior.

Keep VAE shape and decode behavior tested through local unit coverage and image
pipeline validation.
