# DreamX parity fixture

`dreamx-ar-camera.json` is numeric reference output generated by
`scripts/reference-parity/export_dreamx_ar_camera_fixture.py` from the pinned
Apache-2.0
`AMAP-ML/DreamX-World@a1f4c6e5e45600718e5236955f2e0702e53fc275`
trajectory implementation. Its
63 latent / 249 pixel frames use the same `w`, `wj`, `wl` composition and
`4`, `6`, `6` duration weights as the full-resolution upstream-versus-native
gate. It is test data only: the mere.run runtime neither imports nor
redistributes upstream source code.
